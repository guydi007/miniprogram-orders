# 订单地址查看地图

## 当前行为

- 订单详情点击服务地址或「点击查看地图」，云端按城市和地址解析 GCJ-02 坐标。
- 查看匹配结果、确认后打开微信内置地图，后续导航 App 由微信和手机支持情况决定，不固定为高德。
- 城市不一致、无效坐标、精度低于道路级别（level < 7）、可信度不足（reliability < 7）、查询失败或未配置地图服务时，不打开地图，提供复制地址自行搜索的兜底。阈值是本项目的保守策略，字段缺失也不放行。
- 腾讯接口失败时区分签名、权限、并发限流（120）、当日额度（121）、无查询结果（347）、参数和服务异常；额度提示要求检查 Key 的额度分配及用量，不直接要求充值。失败诊断返回安全的数字 `providerStatus` / `httpStatus`，同时以 `[map-geocoder]` 写入云函数日志。失败诊断中绝不返回或记录腾讯原始 message、请求 URL、地址、订单、坐标、Key、SK 或签名；日志失败不改变查询结果。没有错误码时不得猜测额度或地址原因。状态码参考：https://lbs.qq.com/service/webService/webServiceGuide/status 。
- 使用 getOrderLocation 操作，沿用 manageOrder 工单访问权限；只查询云端工单地址，不接受客户端地址覆盖，不修改订单、不发送提醒。
- 返回订单详情时重新获取订单及权限；首次进入只加载一次，并发刷新串行处理。刷新不重置改期、回馈或结算弹窗输入。
- 地图解析不是上门地址准确性的保证：导航前必须核对地图标记，小区入口、楼栋和门牌仍需与客户确认。

## 配置与验证方法

1. 在腾讯位置服务 https://lbs.qq.com 注册开发者，进入控制台创建应用 / Key，启用 WebService API 的地址解析能力并选择「签名校验」，保存同一 Key 配套的 Secret Key（SK）。额度、商用授权及费用以平台当前条款为准。
2. 在微信开发者工具进入云开发控制台，选择当前环境和云函数 manageOrder，在函数配置 / 环境变量中添加下面两个变量并保存（入口名称以实际控制台为准）。不要写进小程序、GitHub、截图或公开文档，也不要发到聊天中：
   - `TENCENT_MAP_KEY`：Key 管理列表中的 Key。
   - `TENCENT_MAP_SECRET`：同一个 Key 的「签名校验」生成的 SK，不是 Key 本身，也不是腾讯云 SecretKey。
3. 部署 manageOrder，确保包含 orderLocation.js；沿用 NOTIFICATIONS.md 要求，云函数总执行超时至少 20 秒。地图请求自身有 5 秒整体超时。
4. 重新编译小程序，手机真机测试完整地址、模糊地址、不同城市、网络失败及地图返回刷新。
5. 本功能不获取师傅当前定位，只提交工单城市和服务地址到腾讯位置服务，不提交客户电话等其他订单字段。上线前应在隐私说明中如实说明第三方地址解析用途；如另加实时定位或地图选点，需另行检查微信权限和隐私配置。

Key 或 SK 缺失时均不发送地图请求，会提示尚未配置，可复制地址；本地模拟检查不能证明真实地图解析成功或导航 App 可用。签名计算遵循腾讯官方规则：参数名排序，使用原始未 URL 编码的值，将请求路径、问号、参数及 SK 拼接后计算 MD5；实际请求才进行 URL 编码，SK 不随请求发送。参考：https://lbs.qq.com/FAQ/server_faq.html 。

## 本次验证（2026-09-13）

- 164 项本地模拟检查通过，其中 35 项覆盖安全错误码诊断；不代表真机导航测试完成。
- manageOrder 安全诊断版本已部署到当前云环境，函数状态 Active，执行超时仍为 20 秒。
- 用户更新配套 SK 并为地址解析分配额度后，同一测试工单连续两次真实云端查询成功，约 0.9 秒和 0.6 秒返回有效 GCJ-02 坐标。未修改订单、未发送提醒，诊断未输出地址或密钥。
- 此前失败没有保存腾讯错误码，本次未复现，不能断言此前根因是额度、权限或地址。再次失败可根据安全诊断码定位。
- 手机真机打开地图、核对标记及导航仍待验证。

## 后续 TODO

- [ ] 完成手机真机打开地图及导航测试（Key/SK 已由用户配置，manageOrder 已部署，真实地址解析已成功）。
- [ ] 按需要增加录单地图选点或已确认坐标，减少文字地址歧义及重复调用。
- [ ] 加入正式的共享缓存、调用频率控制及地图服务用量告警。
- [ ] 重要工单后台提醒单独完善订阅消息 / 企业微信通道，目前订阅入口仍保持隐藏，不能依靠小程序页面后台弹窗提醒。
