# 微信订阅消息 + 可选企业微信群提醒

## TODO：暂缓开放首页订阅入口

- [x] 隐藏首页整块订阅提醒（按钮、说明文字及模板未配置提示），所有角色均不显示；入口关闭时不加载订阅配置，也不申请订阅授权。
- [x] 统一提醒区域的字体、字号、行距和按钮样式：沿用全局字体，说明 26rpx、辅助提示 24rpx、行距 1.6；按钮使用首页的 24rpx 字号、60rpx 高度和绿色辅助按钮风格。仅作用于提醒区域，不修改其他页面。
- [ ] 恢复入口前，在手机真机上确认换行、字号及按钮显示效果。
- [ ] 补齐主管新工单的真实模板 ID 和字段，并核对催单模板。
- [ ] 完善一次性授权用完、发送失败及人工兜底的交互；评估可靠发送与受控重试。
- [ ] 完成角色权限、测试账号隔离及手机真机收发验证后，再启用 index.js 的 notificationEntryEnabled。

本次仅隐藏首页入口，保留后续开发代码；不等于停用云端录单/催单的通知逻辑，也不会撤销微信既有授权。

## 订阅消息配置

- 现在优先发送小程序订阅消息，发送失败才使用该收件人已配置的企业微信 Webhook。未配置 Webhook 不影响已经成功发送的订阅消息。
- 单一配置文件：cloudfunctions/manageOrder/notificationTemplates.json。urgent 沿用原项目催单模板，newOrder 的 templateId 和 fields 暂时留空，必须取得公众平台真实模板和字段后填写，不能冒用催单模板。
- fields 是平台字段名到工单字段的映射，需逐项按真实模板设置；非 thing 类型字段需严格符合平台格式。现有 urgent 使用 thing9=address、thing4=reason。
- miniprogramState 当前是 developer，供开发阶段验证；体验版使用 trial，正式发布改为 formal。
- 首页入口恢复开放后，师傅/主管可点击「订阅微信提醒」授权。主管可订阅新工单和催单模板，师傅可订阅催单模板。普通一次性模板每次接受授权只能发送一条对应消息，勾选保持选择不等于永久无限接收。
- 正式人员使用其绑定的 openid；测试人员使用当前有效的 testSessionOpenid，不写入正式 openid、不使用记录创建者 _openid。测试多台手机时，此版本只向最近建立会话的微信发送。
- 手机真机上验证授权和接收；本地模拟测试不能证明模板可用或微信实际送达。

## 配置和部署

1. 管理员在「员工管理 → 编辑员工」中为师傅和主管填写企业微信群机器人 `webhookUrl`。也可直接在云数据库 users 的员工记录中配置该字段。
2. 主管的 cities 必须包含工单城市。同城市多个主管都会通知；相同 Webhook 合并为一条消息。
3. 用户记录里的手机号应是企业微信群成员实际使用的手机号，才能正确 @ 到本人。该通道是企业微信群机器人，不是个人微信私信。
4. 测试操作只通知 isTest 为 true 的人员，正式操作只通知正式人员。若启用 Webhook，测试人员必须配置测试群地址，禁止使用生产群做测试。
5. **部署前把 manageOrder 云函数执行超时从 3 秒调整到至少 20 秒**。保存数据库、查询接收人员和等待机器人响应都是异步操作；默认 3 秒不足。机器人 HTTP 请求自身有 3 秒的整体截止时间。
6. 上传部署 manageOrder（须包含 groupNotifications.js、notifications.js、notificationTemplates.json 和 config.json 的 subscribeMessage.send 权限）和 login，重新编译小程序；同时部署 sendGroupNotification 和 sendSubscribeMessage 的退役版本，关闭旧的客户端任意推送入口。全部发送已收口到 manageOrder。

## 行为

- createOrder 保存成功后，由云端通知同城市主管。
- urgent 仅客服/主管有权调用；归档订单不可催单。数据库条件更新限制同一工单 60 秒内只接受一次催单。
- 催单使用工单中师傅手机号查接收人；只有没有手机号时才使用姓名。匹配多个员工时不会猜测收件人。
- 群消息不包含完整客户电话，详细信息需在小程序内查看。
- 机器人必须返回 HTTP 200 且 errcode 为 0 才算发送成功。
- 通知失败不会回滚工单，前端会提示人工联系。不要因通知失败重复录单。
- 总通知结果追加到 orders.notificationLogs。Webhook 结果另外写入 groupNotificationLogs，不记录 Webhook 密钥。此日志仅记录发送结果，不表示收件人已阅读。
- 当前没有后台任务队列和自动重试。进程在保存后崩溃或超时，仍可能丢提醒；网络发送成功后日志保存失败也可能出现。后续若要求强可靠性，应增加持久化任务队列和受控重试，而非依赖客户端重发。

## 安全

Webhook 只能是 qyapi.weixin.qq.com 的官方 HTTPS 机器人地址。普通人员不应能读取 users.webhookUrl，请核对云数据库安全规则；小程序内隐藏密码只是显示保护，不代替数据库字段权限。机器人地址等同密钥，应限制管理员可见。此代码修改不会自动变更云数据库安全规则。
